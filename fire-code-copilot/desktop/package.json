{
  "name": "fire-code-copilot-desktop",
  "private": true,
  "version": "1.0.0",
  "description": "Tauri desktop wrapper for Fire Code CoPilot — bundles the UI and manages the local backend.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "icons": "tauri icon src-tauri/icons/icon.png",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
